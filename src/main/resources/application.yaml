# Port Number
server:
  port: 6030 # Conflict occurred with Tomcat in JSP...ㅠㅠ

# 공지사항 크롤링용 FastAPI 서버 주소
cors:
  fastapi-crawler-url: http://localhost:8000 # TODO : 추후에 변경 필요

# mysql -h 119.219.30.209 -P 6040 -u VIRTUKCH -p --default-character-set=utf8 nest_db
spring:
  datasource:
#    url: jdbc:mysql://119.219.30.209:6040/nest_db?serverTimezone=UTC&characterEncoding=UTF-8
#    username: VIRTUKCH
#    password: VIRTUKCH

# mysql -h localhost -P 3306 -u VIRTUKCH -p --default-character-set=utf8 nest_db
#    url : jdbc:mysql://localhost:3306/nest_db?serverTimezone=UTC&characterEncoding=UTF-8
    url: jdbc:mysql://119.219.30.209:6040/nest_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: VIRTUKCH
    password: VIRTUKCH

# mysql -h localhost -P 3306 -u VIRTUKCH -p --default-character-set=utf8 nest_db
#    url : jdbc:mysql://localhost:3306/nest_db?serverTimezone=UTC&characterEncoding=UTF-8
#    username: VIRTUKCH
#    password: VIRTUKCH
#    driver-class-name: com.mysql.cj.jdbc.Driver

#    #지혁
#    url: jdbc:mysql://localhost:3306/nest_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
#    username: nest
#    password: nest

  mail:
    host: smtp.gmail.com
    port: 587
    username: glosori.help@gmail.com
    password: jzdschbukjgnjffz
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
    default-encoding: UTF-8

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

jwt:
  secret: VUkwMS0xMjM0LTU2NzgtOWFiYy1kZWZnaGk=
  access-token-expiration: 3600000  # 1 hour (in milliseconds)
  refresh-token-expiration: 604800000  # 7 days (in milliseconds)

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    config-url: /v3/api-docs/swagger-config
    url: /v3/api-docs
    csrf:
      enabled: false  # ✅ CSRF 설정이 Swagger 요청을 차단할 수 있으므로 비활성화