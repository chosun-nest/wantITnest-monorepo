server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nest_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: VIRTUKCH
    password: VIRTUKCH
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

jwt:
  secret: your_jwt_secret_key
  access-token-expiration: 3600000  # 1?? (??? ??)
  refresh-token-expiration: 86400000  # 24?? (??? ??)
