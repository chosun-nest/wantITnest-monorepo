# ===========================================
# 조선대 IT융합대학 캡스톤 프로젝트
# 공통 환경변수 설정
# ===========================================

# ------------------------------------------
# 프로젝트 기본 정보
# ------------------------------------------
PROJECT_NAME=capstone-design-aws
VERSION=1.0.0

# ------------------------------------------
# 도메인 및 서버 설정
# ------------------------------------------
# 프로젝트 도메인
DOMAIN_NAME=your-project.com

# AWS EC2 서버 IP 설정
# Frontend Server Public IP (Elastic IP)
FRONTEND_SERVER_IP=xxx.xxx.xxx.xxx

# Backend Server Private IP (EC2 생성 후 실제 IP로 수정)
# AWS EC2 콘솔에서 확인: curl -s http://169.254.169.254/latest/meta-data/local-ipv4
BACKEND_SERVER_IP=10.0.10.xxx

# ------------------------------------------
# 서버 포트 설정
# ------------------------------------------
BACKEND_SERVER_PORT=6030
FRONTEND_PORT=80
CRAWLER_PORT=8000

# ------------------------------------------
# 환경 설정
# ------------------------------------------
# 배포 환경 (development/staging/production)
ENVIRONMENT=production

# 로그 레벨 설정
LOG_LEVEL=INFO
LOG_PATH=/app/logs

# ------------------------------------------
# AWS 기본 설정
# ------------------------------------------
AWS_REGION=ap-northeast-2
